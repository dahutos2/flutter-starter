import 'package:flutter/material.dart';

class JudgeView extends StatelessWidget {
  const JudgeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('JudgeView'),
    );
  }
}
